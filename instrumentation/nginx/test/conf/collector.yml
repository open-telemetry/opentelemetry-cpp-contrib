receivers:
  otlp:
    protocols:
      grpc:
exporters:
  debug:
    verbosity: detailed
  file:
    path: /trace.json
processors:
  batch:
service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug, file]
